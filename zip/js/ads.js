// VRealms - ads.js
// Stub pour la gestion pub (AdMob ou autre) côté Capacitor

window.VRAds = {
  showRewardedAd: async () => {
    // Stub : ici tu déclencheras la pub reward (AdMob etc.)
    console.log("showRewardedAd() appelé (stub)");
    return false;
  }
};
